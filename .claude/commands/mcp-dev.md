Start the MCP server in development mode and verify that tools respond:

1. Run `pnpm run mcp` in the background
2. Show the configuration block for claude_desktop_config.json (use `tsx mcp-server.ts`)
3. List the available MCP tools with their description and input schema
4. Suggest the next step based on the current project state
